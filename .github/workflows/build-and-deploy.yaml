name: Build and deploy

on:
  push:
    branches:
      - master
jobs:
  build-publish-docker:
    name: Build and publish docker image
    runs-on: ubuntu-latest
    environment: production
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Build Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_REGISTRY_URL }}:${GITHUB_SHA} .
    - name: Login docker registry
      run: |
        docker login -u ${{ secrets.DOCKER_REGISTRY_USERNAME }} -p ${{ secrets.DOCKER_REGISTRY_PASSWORD }} ${{ secrets.DOCKER_REGISTRY_URL }}
    - name: Publish
      run: |
        docker push ${{ secrets.DOCKER_REGISTRY_URL }}:${GITHUB_SHA}
